var db = require('../db');

module.exports = {
  getAll: function () {
    // db.query(
    //   'SELECT * FROM users',
    //   function (err, results, fields) {
    //     if (err) {
    //       throw (err);
    //     } else {
    //       callback(err, results);
    //     }
    //   }
    // );
  },
  create: function () {}
};
